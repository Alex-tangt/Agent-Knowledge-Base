2026.7.24
与AI共同设计的查询改写prompt实验问题。
1、实验设计

实验设计的颗粒度：当你留下了空白，AI就会以不确定性填补它。

评价指标：必须详细清楚每个指标是如何计算的，对应什么核心观点，这个指标的可信度。

2、实验记录

实验报告的颗粒度：实验报告是我review 做的一个实验的根本依据，它因该包含丰富而全面的信息，让我不至于漏掉关键点

3、整个实验的章程

第一次查询重写优化实验失败归因分析

为什么一开始小批量假设验证做的很好，后面直接调到循环调优了呢？这真的有必要吗，在还没跑通流程前先考虑批量优化？

对实验的设计太粗糙，只粗略的敲定了实验逻辑、评价指标/损失函数也是简简单单口头写的。但是关于这种问题损失函数设计我确实不太懂

其实在实践中，LLM分解问题的思路完全与我我的想法背道而驰，我想的是把一个问题分解为几个子问题，而它是直接换了几种写法。其实，我认为的问题改写首先是与知识库相关的语义替换（但是表意不变就行），二是需要推理，像cot一样把一个复杂问题拆解为几个小问题


问题：

其实与AI讨论实验的过程中已进有了许多错误的源头，但是我没注意或者选择性忽略了。

第一次实验设计中，主session中提到最好chunk低到0.0041也会拒绝回答，我没有仔细看具体的chunk与问题，没有验证直接跳过了，认为是查询改写的问题；为什么可以未经验证而闭掉最有问题的方向呢？

在查询改写一开始设定方案时，主session提到了改写规则当前只有一条，len(query)<20跳过改写，因为代码不是我写的、我对于整个RAG项目的具体代码实现不清楚，没意识到这是一个会过滤掉改写的系统层面逻辑bug，表达简单不代表问题简单不需要改写，而是直接忽略了。

pool=100与pool=20关于检索覆盖率假设的验证：核心逻辑假如最终Reranker后前八条在20至100之间的概率为遗漏率，这里验证的链路是query到检索结果。但是这个假设做完后，为什么后续实验一和实验二pool任然设为100没有恢复呢？这极大的增加了reranker阶段的时耗。其实我现在对这个假设验证的到底是pool池的大小还是检索覆盖率还是有点想不明白？


严重的问题

实验数据、概念验证数据的记录有大问题，让我review变得很困难，只能从头到尾看对话记录。

对齐颗粒度的问题