# 0002 版本管理：原地基线重置

Status: accepted

保留仓库与远程 URL，把 2026-07-12 以来三周未提交的工作整理后重置为干净的初始基线，之后所有工作按"单元"小步提交。基线 = "敢签名的状态"（已知限制在 ADR 或文档中声明），不是现状快照。

理由：历史仅有 4 个课程演化 commit 且大部分工作 untracked，无保留价值；干净的基线是可上简历的叙事起点。

Consequences: 强制推送重写 `origin/master` 历史（个人仓库，可接受）。基线创建前必须完成清理与质量修复（延迟、拒答），让基线上能看到"修复前 → 修复后"的完整弧线。
