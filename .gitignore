# Python
#
*.py[cod]
*$py.class
__pycache__/
.Python
*.so
.Python

# Virtual Environment
#
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
#
.idea/
.vscode/
*.swp
*.swo
*~

# OS
#
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
#
*.log
logs/

# Environment variables
#
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Secrets — never commit (keys / tokens / passwords / credentials)
#
*.secret
secrets/
*credential*
*password*

# Build artifacts
#
build/
dist/
*.egg-info/

# Generated vector database (rebuild via legal_web/ingest.py)
#
legal_web/vector_db/

# Generated memory index (rebuild via memory_agent/build_index.py)
#
memory_agent/vector_db/

# Local read-only corpus repo list (machine-specific absolute paths).
# Copy readonly_repos.example.json to readonly_repos.json and edit.
#
memory_agent/readonly_repos.json

# Local ingestion overlay (include/exclude path patterns); machine-specific.
# Copy overlay.example.json to overlay.json and edit.
#
memory_agent/overlay.json

# Runtime uploads
#
legal_web/uploads/

# Uploaded document originals (D9) + materialized read-only corpus md (#51).
# Truth source is the materialized Markdown; originals are gitignored attachments.
memory_agent/uploads/
memory_agent/imports/

# Dedicated document-parsing environment (Docling + torch); NEVER the main venv
# (ADR-0027 D7 / #51). Create with `python -m venv venv-parse`.
venv-parse/

# External benchmark dataset (MultiHop-RAG, ODC-BY) — not committed (issue #47)
#
experiments/agentic-rag-census/data/
experiments/agentic-rag-census/store/
experiments/agentic-rag-census/artifacts/eval_set.json
experiments/agentic-rag-census/artifacts/trace/

# Trae IDE
#
.trae/

# Windows
#
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# macOS
#
.DS_Store
.AppleDouble
.LSOverride

# Linux
#
*~

# Temporary files
#
*.tmp
*.temp
.tmp/
.temp/